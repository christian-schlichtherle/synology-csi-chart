apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "synology-csi.fullname" . }}-test-connection"
  labels:
    {{- include "synology-csi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "synology-csi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
