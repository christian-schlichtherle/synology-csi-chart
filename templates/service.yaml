apiVersion: v1
kind: Service
metadata:
  name: {{ include "synology-csi.fullname" . }}
  labels:
    {{- include "synology-csi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "synology-csi.selectorLabels" . | nindent 4 }}
